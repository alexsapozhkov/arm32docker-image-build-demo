name: Build

on: 
  push:
    branches: 'main'

jobs:
  build-docker-macos:
    runs-on: macos-12
    steps:
    - run: |
        brew install docker-machine docker
        sudo docker –v
        echo "testing without sudo"
        docker –v
        docker build . --tag arm32demo
