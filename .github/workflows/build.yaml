name: Build

on: 
  push:
    branches: 'main'

jobs:
  my_job:
    runs-on: [macOS-latest]
    steps:
      - name: echo
        run: |
          brew install docker-machine docker
          sudo docker –version